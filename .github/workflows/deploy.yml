name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
        env:
          GH_PAGES_REPO: ${{ github.event.repository.name }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
=======
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build
        env:
          GH_PAGES_REPO: ${{ github.event.repository.name }}

      - uses: actions/upload-pages-artifact@v3
>>>>>>> f3a014448d65f03a230e9202dfa4a78fd4ef83d0
        with:
          path: dist

  deploy:
<<<<<<< HEAD
    needs: build
    runs-on: ubuntu-latest
=======
    runs-on: ubuntu-latest
    needs: build
>>>>>>> f3a014448d65f03a230e9202dfa4a78fd4ef83d0
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
<<<<<<< HEAD
      - name: Deploy
        id: deployment
=======
      - id: deployment
>>>>>>> f3a014448d65f03a230e9202dfa4a78fd4ef83d0
        uses: actions/deploy-pages@v4
